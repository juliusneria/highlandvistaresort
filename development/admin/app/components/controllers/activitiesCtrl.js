angular
    .module('Admin')
    .controller('activitiesCtrl',activitiesCtrl);
activitiesCtrl.$inject = ['$scope','$timeout','$state','$localStorage'];
function activitiesCtrl($scope, $timeout, $state,$localStorage) {

}